def somefunction():
    #do some stuff
    #some recursive case
    somefunction()
#some recursive case 
#    #stack trace of instructions, a stack appears if something cannot be solved

    def add(num, num2)
        return num + num2
    add (1,2) #3

    #some recursive case and some base case

    def factorial(num) #4
        if (num <= 1):
            return 1
        else:
            return num * factorial(num - 1)
        
        fib * 

def fib(number):
    #base case
    if (number = 1 or number = 2):
        return 1
    else:
        return fib(number -1) + fib(number - 2)
